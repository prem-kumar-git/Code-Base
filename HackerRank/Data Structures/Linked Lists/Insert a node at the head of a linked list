Node Insert(Node head,int x) {
    if(head == null){
        head.data = x;
        head.next = null;
    }
    else{
        Node newNode = new Node();
        newNode.data = x;
        newNode.next = head;
        head = newNode;
    }
    return head;
}
